import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primaryBlue = Color(0xFF000080);
  static const Color primaryDarkBlue = Color(0xFF00005A);
  static const Color accentGold = Color(0xFFD4AF37);
  static const Color greenAccent = Color(0xFF059669);
  
  // Backgrounds
  static const Color mainBackground = Color(0xFF0F0F14);
  static const Color lightBackground = Color(0xFFF5F5F8);
  
  // Cards
  static const Color cardBackground = Color(0xFF1E1E2D);
  static const Color lightCard = Colors.white;
  
  // UI Elements
  static const Color circleButton = Colors.white10;
  static const Color darkNav = Color(0xFF1E1E2D);
}
