import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text("heloword")),
        body: Center(child: Text("ali weka")),
        floatingActionButton: FloatingActionButton(onPressed: () {}),
      ),
    ),
  );
}
